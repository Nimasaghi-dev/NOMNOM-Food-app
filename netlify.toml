[build]
  base = "client"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA routing: send all paths to index.html so React Router can handle them.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
